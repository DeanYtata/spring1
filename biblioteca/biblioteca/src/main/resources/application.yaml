server:
  port: 8082

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/biblioteca_db
    username: reto
    password: reto
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true